<resources>
    <!-- Base application theme. -->
    <style name="AppTheme1" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/cyanbase</item>
        <item name="colorPrimaryDark">@color/cyanbase</item>
        <item name="colorAccent">@color/cyanbase</item>
        <item name="fontFamily">@font/coustard</item>
        <item name="fontWeight">1</item>
        <item name="android:textSize">12dp</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SwitchThemeGREEN" parent="Theme.AppCompat.Light">
        <item name="android:colorControlActivated">@color/cyanbase</item>
    </style>

    <style name="withoutWindowBarTheme" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/cyanbase</item>
        <item name="colorPrimaryDark">@color/cyanbase</item>
        <item name="colorAccent">@color/cyanbase</item>
        <item name="fontFamily">@font/coustard</item>
        <item name="fontWeight">1</item>
        <item name="android:textSize">12dp</item>
        <item name="android:fontFamily">@font/coustard</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="android:windowTranslucentNavigation">@color/cyanbase</item>


    </style>

</resources>